# ubuntu-dev-setup

cập nhật danh sách các gói phần mềm mới nhất cùng phiên bản của chúng từ kho lưu trữ. Mục đích để hệ thống biết được những thay đổi mới nhất và có thể cài đặt hoặc nâng cấp các phần mềm một cách an toàn và chính xác.

```update
sudo apt update
```

đàu tiên và không thể thiếu khi ta gõ chữ trên các hệ điều hành.
✓ Vietnamese input (IBus unikey)

✓ git
✓ curl
✓ VS Code
trước khi cài bằng lệnh bash trước tiên cần cài curl
```surl
sudo apt install curl
```

```bash
bash -c "$(curl -fsSL https://raw.githubusercontent.com/foxrolong/ubuntu-dev-setup/main/install.sh)"
```

✓ Brave

